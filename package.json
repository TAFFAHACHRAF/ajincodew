{
  "name": "ajincodew-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next-sitemap && next export",
    "start": "next start",
    "export": "next export"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
